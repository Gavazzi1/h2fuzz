mutable_fields: {
  data = ();
  headers = (("name", "header"), ("value", "header"), ("encoding", "header"),
             ("dup", "header"), ("swap", "header"), ("delete", "header"));
  priority = ();
  rst_stream = ();
  settings = ();
  push_prom = (("name", "header"), ("value", "header"), ("encoding", "header"),
               ("dup", "header"), ("swap", "header"), ("delete", "header"));
  ping = ();
  goaway = ();
  win_update = ();
  continuation = (("name", "header"), ("value", "header"), ("encoding", "header"),
                  ("dup", "header"), ("swap", "header"), ("delete", "header"));
};

likelihoods: {
  mutate_hdr_settings = 75;

  mutate_operators: {
    bit = 75;
    delete = 5;
    dup = 10;
    swap = 10;
  }

  crossover_operators: {
    add = 50;
    splice = 50;
  }
}