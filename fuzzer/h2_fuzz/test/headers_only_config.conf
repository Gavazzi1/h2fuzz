mutable_fields: {
  data = ();
  headers = (("flags", "base"), ("reserved", "base"), ("streamid", "base"),
             ("padflag", "pad"), ("length", "pad"), ("padding", "pad"),
             ("priorityflag", "headers"),
             ("exclusive", "depweight"), ("streamid", "depweight"), ("weight", "depweight"),
             ("name", "header"), ("value", "header"), ("dup", "header"), ("swap", "header"), ("encoding", "header"));
  priority = ();
  rst_stream = ();
  settings = ();
  push_prom = (("flags", "base"), ("reserved", "base"), ("streamid", "base"),
               ("padflag", "pad"), ("length", "pad"), ("padding", "pad"),
               ("name", "header"), ("value", "header"), ("dup", "header"), ("swap", "header"),
               ("reserved", "push_prom"), ("streamid", "push_prom"), ("encoding", "header"));
  ping = ();
  goaway = ();
  win_update = ();
  continuation = (("flags", "base"), ("reserved", "base"), ("streamid", "base"),
                  ("name", "header"), ("value", "header"), ("dup", "header"), ("swap", "header"), ("encoding", "header"));
};

likelihoods: {
  mutate_hdr_settings = 75;

  mutate_operators: {
    bit = 75;
    delete = 5;
    dup = 10;
    swap = 10;
  }

  crossover_operators: {
    add = 50;
    splice = 50;
  }
}